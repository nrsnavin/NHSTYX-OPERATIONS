# NH Styx Operations — environment
# Copy to `.env` and adjust. All client-exposed vars must be prefixed VITE_.

# Base URL of the NH Styx backend API (including version prefix).
VITE_API_URL=http://localhost:4000/api/v1
